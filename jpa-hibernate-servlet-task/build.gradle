group 'com.bobocode'
version '1.0'

apply plugin: 'war'

dependencies{
    compile project(':jpa-hibernate-util')

    compile "javax.servlet:javax.servlet-api:3.1.0"
    compile "javax.servlet:jstl:1.2"
}